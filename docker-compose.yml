version: '3.8'

services:

  api:

    build: .

    ports:
      - "3000:3000"

    volumes:

      - ./data:/app/data

    environment:
      - PORT=3000
      - JWT_SECRET=sua-chave-secreta-super-forte-12345
